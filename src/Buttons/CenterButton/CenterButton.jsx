import React from 'react'

const centerButton = (props) => {
    return (
        <button 
        disabled = {props.disabled} 
        >
        =
        </button>
    );
};

export default centerButton;